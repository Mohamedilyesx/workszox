/* =====================================================
   JAWAK TV — TJAR PLATFORM REDESIGN
   jawaktv-tjar.css  |  v3.0  |  2026
   ===================================================== */

/* === VARIABLES === */
:root {
  --g:    #daae49;
  --gl:   #f0d878;
  --gd:   #c9922a;
  --gx:   #b88b3c;
  --bg:   #1a1815;
  --bg2:  #1f1c19;
  --bg3:  #242018;
  --bg4:  #141109;
  --txt:  #f0ead8;
  --txs:  #c8bfa8;
  --txm:  #908778;
  --bdr:  rgba(218,174,73,.18);
  --ease: cubic-bezier(0.16, 1, 0.3, 1);
  --bar-h: 42px;
  --nav-h: 65px;
}


.prevBtn-SD5kV i {
  color: #000 !important;
}



/* ===تمارا وضع ليلي   === */

/* الكونتينر الرئيسي */
.tamara-summary-widget--inline-outlined:hover {
    background-color: #c29540!important;
}
/* ===تقييم باللون ذهبي مع نجمة  === */

.sr_rating {
    color: #daae49 !important;
}

.sr_rating i.la-star,
.sr_rating .las.la-star {
    color: #daae49 !important;
}

/* ===وضع ليلي تقييماتت  === */

.rv2-list-item {
    background: #242018;
}

.rv2-date,
.rv2-total {
    background-color: #daae49 !important;
    color: #fff; /* لون النص */
    padding: 4px 10px;
    border-radius: 8px;
    display: inline-block;
    font-weight: 600;
}

.offcanvas-body {
    background-color: transparen!important;
}

/* ===fix border === */
.widgetArea.slider-widget-area {
    border: transparent!important;
}
.widgetArea, section.widgetArea {
    border: transparent!important;
}

/* ===dark mode  border === */
.sign-in {
    background: #231d15 !important;

}

/* === GLOBAL DARK BASE === */
html { background-color: var(--bg) !important; }

body {
  background-color: var(--bg) !important;
  color: var(--txt) !important;
}

/* === fix .store-isotope-list  color  === */

.store-isotope-list .list {
    color: white!important;
}

.arrow-box button {
    color: #ffffff!important;
}


/* ===fix some prolbem in cart page   === */
.la, .las {
    color: #7b7b7b!important;
}


.admin .msg_details {
    color: #fff!important;
    background: #735d2d!important;
}

/* === checkout some fix  === */
.coupon_promo-input .btn-copy, .coupon_promo-input .btn.btn-main {
    border-radius: 10px !important;
}
.coupon_promo-input {
    border-radius: 1rem!important;
   background: #463b22!important;
}

.bank_details {
    background: #463b22!important;
}

.uploader {
    background: #242018!important;
}

.payment_tab .img img {
    background: #e5c462!important;
    padding: 2px!important;
    border-radius: 10px!important;
}

/* === checkout dark mode   === */

.order_left, .order_right {
    background: #242018!important;
    color: #f0ead3!important;
}

.order_list-content {
    color: #ffffff!important;
}
.order_total li span {
    color: #ffffff!important;
}

.order_total li.price-total {
    background: #463b22!important;
}

.quantity-product {
    background: #463b22!important;
}

/* ===order-review dark mode   === */

.order-header {
    background: #242018!important;
    padding: 32px!important;
    border-radius: 20px!important;
}

.contact-info-wrap > div {
    background: #242018!important;
}

.rv_floating-footer {
    background: rgb(70 59 34)!important;
}

.p-4 {
    background: #242018!important;
}
.rv_item-head {
    background: #242018!important;
}

[dir=rtl] .order-header > .rv_btn {
        margin-right: auto;
        background: #463b22!important;
}
.rating_txt {
    background: #463b22!important;
}
.order-id {
    background: #463b22!important;
}

.rv_btn {
    background: #dfb753!important;
    color: #000000!important;
}
.rv_badg_num {
    background: transparent!important;
    border: transparent!important;
}

/* ===wishlist dark mode   === */
.cart-item-wrap {
    background: #242018;
    color: white!important;
    border: transparent!important;
}

.cart-item-price {
    color: #ffffff!important;
}
/* ===pop up cart and wishlist and user dark mode   === */
[dir="rtl"] .custom-navbar .dropdown-contents.dropdown-left {
    background: #463b22!important;
}

/* ===manage-account dark mode   === */
.dash_table-wrap tbody td {
    background: #242018!important;

}

.dash_btn.dash_table-btn {
    background: #e8ca68!important;
    color: #000000!important;
}

[dir=rtl] .dash_table-wrap thead th:first-child, [dir=ltr] .dash_table-wrap thead th:last-child, [dir=rtl] .dash_table-wrap tbody td:first-child, [dir=ltr] .dash_table-wrap tbody td:last-child {
    background: #242018!important;
}

.manage_account-head {
    background: #232323!important;
}

.dash_body-head span {
    color: #ffffff!important;
}

.manage_account-row {
    color: #ffffff!important;
}

.manage_account-img span {
    color: #ffffff!important;
}
.manage_account-list li span {
    color: #ffffff!important;
}

.manage_account-head .dash_btn {
    color: #000000!important;
    background: #ebcd6c!important;
}
.form-group label {
    color: #ffffff!important;
}

.dash_modal-header span {
    color: #ffffff!important;
}

.dash_btn {
    background: #daae49;
    color: #000000!important;
}

/* ===pop up cart and wishlist and user dark mode   === */
.support_ticket-view .dash_body-head, .refund_chat-view .dash_body-head {
    background: #1f1c19!important;
}

.ticket_msg-wrap .title {
    background: #1f1c19!important;
}

.ticket_info {
    color: #ffffff!important;
}

.ticket_msg {
 color: #ffffff!important;
}

.mt-1 {
    color: #ffffff!important;
}
.dash_modal label, .form-label {

    color: #ffe8b4!important;
}

.ticket_msg-wrap .title {
    background: #1f1c19!important;
}

.msg_area-btn 
    background: #daae49!important;
}

.dash_body-head-title, .manage_account-title {
    color: #ffffff!important;
}

.ticket_msg-wrap .title {
    color: #ffffff!important;
}


/* ===order_details dark mode   === */

.order_details {
    background: #1f1c19!important;
    border: 1px solid var(--br-clr)!important;
    color: #ffffff!important;
    border: solid 0.5px #ddb44f!important;
}

.order_head .order_date {
    background: #715621!important;
}

.order_list-title {
    border: transparent!important;
}

.order_list li {
    border: 1px solid #060605!important;
}


  /* ── WhatsApp floating button ──
._wa {
    width: 50px !important;
    height: 50px !important;
    background-color: #2cd46b !important;

}


.cart-item-img img {
    aspect-ratio: auto!important
}

/* Headings */
h1, h2, h3, h4, h5, h6 { color: var(--txt) !important; }

/* Links */
a { color: var(--txs); text-decoration: none; transition: color .2s var(--ease); }
a:hover { color: var(--g); }

/* Scrollbar */
::-webkit-scrollbar { width: 5px; height: 5px; }
::-webkit-scrollbar-track { background: var(--bg4); }
::-webkit-scrollbar-thumb { background: rgba(218,174,73,.3); border-radius: 3px; }
::-webkit-scrollbar-thumb:hover { background: rgba(218,174,73,.5); }

/* Inputs */
input:not([type="range"]), textarea, select {
  background-color: var(--bg3) !important;
  border: 1px solid rgba(218,174,73,.2) !important;
  color: var(--txt) !important;
  border-radius: 10px !important;
}
input:focus, textarea:focus, select:focus {
  border-color: rgba(218,174,73,.5) !important;
  outline: none !important;
  box-shadow: 0 0 0 3px rgba(218,174,73,.08) !important;
}
input::placeholder, textarea::placeholder { color: var(--txm) !important; }

/* HR lines */
hr {
  border-color: rgba(218,174,73,.12) !important;
  opacity: 1 !important;
}

/* Bootstrap overrides — background-color ONLY, never background shorthand */
.bg-white  { background-color: var(--bg2) !important; }
.bg-light  { background-color: var(--bg3) !important; }
.text-dark { color: var(--txt) !important; }
.text-muted, .text-secondary { color: var(--txm) !important; }
.border    { border-color: var(--bdr) !important; }
.border-top    { border-top-color: var(--bdr) !important; }
.border-bottom { border-bottom-color: var(--bdr) !important; }

/* ============================================================
   PROMO BAR — #jwk-bar (built by JS)
   Fixed at absolute top, z:100000
   ============================================================ */
.promobar, .promobar.container-fluid { display: none !important; }

#jwk-bar {
  position: fixed !important;
  top: 0 !important; left: 0 !important; right: 0 !important;
  height: var(--bar-h);
  z-index: 100000 !important;
  display: flex;
  align-items: center;
  overflow: hidden;
  background: linear-gradient(135deg, #0e0901, #1c1200, #221400, #0e0901);
  border-bottom: 1px solid rgba(218,174,73,.22);
  box-shadow: 0 2px 16px rgba(0,0,0,.6), inset 0 1px 0 rgba(218,174,73,.08);
  box-sizing: border-box;
}
#jwk-bar::before {
  content: '';
  position: absolute; top: 0; left: 0; right: 0; height: 1px;
  background: linear-gradient(90deg, transparent, rgba(218,174,73,.6) 50%, transparent);
  pointer-events: none;
}
#jwk-mask {
  flex: 1; height: 100%; overflow: hidden;
  -webkit-mask-image: linear-gradient(90deg, transparent, #000 5%, #000 95%, transparent);
  mask-image: linear-gradient(90deg, transparent, #000 5%, #000 95%, transparent);
}
#jwk-track {
  display: inline-flex; flex-wrap: nowrap;
  height: 100%; align-items: center;
  white-space: nowrap; will-change: transform;
}
.jwk-item {
  display: inline-flex; align-items: center;
  padding: 0 26px; color: #e8dfc0;
  font-size: 13.5px; font-weight: 500; white-space: nowrap;
  direction: rtl; transition: color .2s;
}
.jwk-item:hover { color: #f5e9aa; }
.jwk-sep {
  display: inline-flex; align-items: center;
  padding: 0 4px; color: rgba(218,174,73,.45); font-size: 9px;
}
#jwk-close {
  flex: 0 0 38px; width: 38px; height: 100%;
  display: flex; align-items: center; justify-content: center;
  background: transparent; border: none; cursor: pointer;
  color: rgba(218,174,73,.6); font-size: 15px;
  transition: color .2s, opacity .2s; opacity: .65;
}
#jwk-close:hover { color: var(--g); opacity: 1; }

/* ============================================================
   NAVBAR — fixed below promo bar, glass on scroll
   ============================================================ */
nav.navbar.navbar3, .custom-navbar,
.navbar.navbar3 {
  position: fixed !important;
  top: var(--bar-h) !important;
  left: 0 !important; right: 0 !important;
  z-index: 9999 !important;
  background-color: transparent !important;
  border-bottom: none !important;
  box-shadow: none !important;
  transition: background-color .3s ease, backdrop-filter .3s ease,
              box-shadow .3s ease, top .3s ease !important;
}
/* Bar closed → navbar snaps to top */
body.jwk-bar-closed nav.navbar.navbar3,
body.jwk-bar-closed .custom-navbar,
body.jwk-bar-closed .navbar.navbar3 {
  top: 0 !important;
}
/* Scrolled → dark glass */
nav.navbar.navbar3.scrolled, .custom-navbar.scrolled,
.navbar.navbar3.scrolled {
  background-color: rgba(10,7,0,.93) !important;
  backdrop-filter: blur(18px) saturate(1.4) !important;
  -webkit-backdrop-filter: blur(18px) saturate(1.4) !important;
  border-bottom: 1px solid rgba(218,174,73,.15) !important;
  box-shadow: 0 4px 28px rgba(0,0,0,.5) !important;
}

/* Navbar text/icons */
nav.navbar.navbar3 .navbar-nav li a,
nav.navbar.navbar3 .nav-link,
nav.navbar.navbar3 a,
.custom-navbar .nav-link,
.custom-navbar a {
  color: #fff !important;
  font-weight: 600;
  transition: color .2s !important;
}
nav.navbar.navbar3 .navbar-nav li a:hover,
nav.navbar.navbar3 .nav-link:hover,
.custom-navbar .nav-link:hover,
.custom-navbar a:hover { color: var(--g) !important; }

nav.navbar.navbar3 svg, nav.navbar.navbar3 i,
.custom-navbar svg, .custom-navbar i { color: #fff !important; }
.icon-notification { background-color: var(--g) !important; color: #000 !important; }

/* Breadcrumb */
.breadcrumb { background-color: transparent !important; }
.breadcrumb-item a { color: var(--txm) !important; }
.breadcrumb-item.active { color: var(--txs) !important; }
.breadcrumb-item + .breadcrumb-item::before { color: var(--txm) !important; }

/* ============================================================
   HERO — fullscreen video (added by JS)
   ============================================================ */
.hero.hero-area, .hero-area {
  position: relative !important;
  overflow: hidden !important;
  min-height: 100vh !important;
  background-color: #000 !important;
  display: flex !important;
  align-items: center !important;
}

/* Hide Tjar's static hero image */
.hero.hero-area .hero-img-wrap { display: none !important; }

.jwk-hero-video {
  position: absolute !important;
  inset: 0 !important;
  width: 100% !important; height: 100% !important;
  object-fit: cover !important;
  z-index: 0 !important;
  pointer-events: none !important;
}
.jwk-hero-overlay {
  position: absolute !important;
  inset: 0 !important;
  background: linear-gradient(
    to bottom,
    rgba(0,0,0,.78) 0%,
    rgba(0,0,0,.3) 40%,
    rgba(0,0,0,.15) 60%,
    rgba(0,0,0,.6) 100%
  ) !important;
  z-index: 1 !important;
  pointer-events: none !important;
}
.hero.hero-area .container,
.hero.hero-area .intro-excerpt {
  position: relative !important; z-index: 2 !important;
}
.hero.hero-area .intro-excerpt h1,
.hero.hero-area .intro-excerpt h2 {
  color: #fff !important;
  text-shadow: 0 2px 24px rgba(0,0,0,.8);
}
.hero.hero-area .intro-excerpt p {
  color: #d4cbb0 !important;
  text-shadow: 0 1px 8px rgba(0,0,0,.6);
}
.hero.hero-area .intro-excerpt a,
.hero.hero-area .btn-hero,
.hero.hero-area button:not(#jwk-close) {
  background: linear-gradient(135deg, var(--gx), var(--g), var(--gl)) !important;
  border: none !important;
  color: #0a0800 !important; font-weight: 800 !important;
  border-radius: 999px !important; padding: 14px 36px !important;
  box-shadow: 0 10px 30px rgba(218,174,73,.3) !important;
  transition: transform .25s var(--ease), box-shadow .25s var(--ease) !important;
  text-decoration: none !important;
}
.hero.hero-area .intro-excerpt a:hover { transform: translateY(-3px) !important; }

/* ============================================================
   HERO bottom Splide slider (banner images below hero)
   ============================================================ */
.slider-widget-area {
  background-color: var(--bg) !important;
}
.slider-widget-area .splide__slide { background-color: transparent !important; }
.slider-widget-area .splide__arrows .splide__arrow {
  background-color: rgba(26,24,21,.8) !important;
  border: 1px solid var(--bdr) !important;
}
.slider-widget-area .splide__arrows .splide__arrow svg { fill: var(--g) !important; }

/* ============================================================
   PRODUCT CARDS  (.product-item.card2)
   CRITICAL: background-color NOT background to preserve img tags
   ============================================================ */
/* Hide ratings */
.product-item .rating-wrap,
.card2 .rating-wrap,
.d-flex.justify-content-center.align-items-center.m-0.p-0 > .rating-wrap {
  display: none !important;
}

.product-item, .card2, .product-item.card2 {
  background-color: var(--bg2) !important;  /* ← background-color, not background */
  border: 1px solid rgba(218,174,73,.14) !important;
  border-radius: 16px !important;
  box-shadow: 0 4px 16px rgba(0,0,0,.3) !important;
  transition: transform .3s var(--ease), border-color .3s var(--ease), box-shadow .3s var(--ease) !important;
}
.product-item:hover, .card2:hover {
  transform: translateY(-5px) !important;
  border-color: rgba(218,174,73,.35) !important;
  box-shadow: 0 14px 36px rgba(218,174,73,.1), 0 6px 20px rgba(0,0,0,.4) !important;
}

/* Product thumbnail - NEVER hide or filter product images */
.product-item .product-thumbnail-wrap,
.card2 .product-thumbnail-wrap {
  display: block !important;
  background-color: var(--bg3) !important;
}
.product-item img, .card2 img {
  /* NO filter, NO display:none — images must show */
  display: block !important;
}

/* Product title — h3.product-title > a.text-break */
h3.product-title,
h3.product-title a,
h3.product-title a.text-break,
.product-item .product-title,
.product-item .product-title a,
.card2 .product-title,
.card2 .product-title a {
  color: var(--txt) !important;
  font-weight: 700 !important;
  transition: color .2s !important;
}
.product-item:hover .product-title a,
.card2:hover .product-title a { color: var(--g) !important; }

/* Price */
.product-item .product-price, .card2 .product-price,
strong.product-price { color: var(--g) !important; font-weight: 700 !important; }
.product-item .old-price, .card2 .old-price {
  color: var(--txm) !important; text-decoration: line-through !important;
}

/* Cart / Wishlist buttons on cards */
.cart_btn2-border, .cart_btn2 .cart_btn2-border {
  background-color: transparent !important;
  border: 1px solid rgba(218,174,73,.45) !important;
  border-radius: 999px !important;
  color: var(--g) !important;
  transition: all .25s var(--ease) !important;
}
.cart_btn2:hover .cart_btn2-border {
  background: linear-gradient(135deg, var(--gx), var(--g), var(--gl)) !important;
  border-color: transparent !important;
  color: #0a0800 !important;
}
.cart_btn2-txt { color: inherit !important; }
.cart_btn2-icon { color: inherit !important; fill: currentColor !important; }

.add-to-wishlist-btn, .wish-icon {
  background-color: rgba(26,24,21,.85) !important;
  border: 1px solid rgba(218,174,73,.3) !important;
  color: var(--g) !important;
  border-radius: 50% !important;
}
.add-to-wishlist-btn .heart { color: var(--g) !important; }

/* ============================================================
   BRAND IMAGES — ONLY .brand-img img gets filter, NOTHING else
   ============================================================ */
.brand-area, .brand-area.widgetArea {
  background-color: transparent !important;
}
.brand-img {
  background: linear-gradient(135deg, var(--bg2), var(--bg3)) !important;
  border: 1px solid rgba(218,174,73,.15) !important;
  border-radius: 14px !important;
  padding: 14px 22px !important;
  display: flex !important; align-items: center !important; justify-content: center !important;
  transition: border-color .25s ease, box-shadow .25s ease !important;
}
.brand-img:hover {
  border-color: rgba(218,174,73,.35) !important;
  box-shadow: 0 6px 18px rgba(218,174,73,.08) !important;
}
/* ONLY brand-img images get the filter — NOT widgetArea images globally */
.brand-img img {
  filter: brightness(0) invert(1) !important;
  opacity: .9 !important;
  max-height: 150px !important; 
  width: auto !important;
  object-fit: contain !important;
  transition: opacity .25s ease !important;
}
.brand-img:hover img { opacity: .9 !important; }

/* ============================================================
   FEATURES  (.four-blocks-box .single-promo)
   Correct selectors from actual HTML
   ============================================================ */
.four-blocks, .promo-area {
  background-color: var(--bg) !important;
}
.four-blocks-grid {
  display: grid !important;
  grid-template-columns: repeat(auto-fit, minmax(220px, 1fr)) !important;
  gap: 20px !important;
}

.four-blocks-box { height: 100%; }

.four-blocks-box .single-promo,
.promo-area .single-promo {
  background: linear-gradient(145deg, var(--bg2), #201d19) !important;
  border: 1px solid rgba(218,174,73,.15) !important;
  border-radius: 20px !important;
  padding: 28px 24px !important;
  height: 100% !important;
  box-sizing: border-box !important;
  display: flex !important; flex-direction: column !important; align-items: center !important;
  text-align: center !important; position: relative; overflow: hidden;
  transition: transform .3s var(--ease), border-color .3s var(--ease), box-shadow .3s var(--ease) !important;
  box-shadow: 0 4px 18px rgba(0,0,0,.2) !important;
}
.four-blocks-box .single-promo::before {
  content: '';
  position: absolute; inset: 0;
  background: radial-gradient(ellipse at top, rgba(218,174,73,.05), transparent 70%);
  pointer-events: none;
}
.four-blocks-box .single-promo:hover {
  transform: translateY(-4px) !important;
  border-color: rgba(218,174,73,.3) !important;
  box-shadow: 0 12px 34px rgba(218,174,73,.1), 0 6px 18px rgba(0,0,0,.35) !important;
}

/* Icon container */
.single-promo-icon {
  display: flex !important; align-items: center !important; justify-content: center !important;
  width: 60px !important; height: 60px !important; border-radius: 16px !important;
  background-color: rgba(218,174,73,.1) !important;
  border: 1px solid rgba(218,174,73,.2) !important;
  margin-bottom: 18px !important; flex-shrink: 0 !important;
  color: var(--g) !important;
}
.single-promo-icon i { color: var(--g) !important; font-size: 26px !important; }
.single-promo-icon svg { stroke: var(--g) !important; width: 26px !important; height: 26px !important; }

/* Feature title and description */
.single-promo-contents-title,
h4.single-promo-contents-title {
  color: #fff3cf !important; font-weight: 700 !important; margin-bottom: 8px !important;
}
.single-promo-contents-para,
p.single-promo-contents-para {
  color: var(--txs) !important; font-size: 14px !important; line-height: 1.8 !important;
}

/* Section headings above features */
.four-blocks .section-head .section-title,
.four-blocks .section-title {
  color: var(--txt) !important;
}
.four-blocks .section-head .section-sub {
  color: var(--g) !important;
}

/* ============================================================
   ABOUT / INTERACTIVE BLOCK (added by JS)
   ============================================================ */
.about-area { background-color: transparent !important; }
.about-area .about-box { display: none !important; }

.jawak-interactive-block {
  padding: 80px 0;
  direction: rtl;
  background: radial-gradient(ellipse at 50% 0%, rgba(218,174,73,.07), transparent 55%),
              linear-gradient(180deg, #1a1815, #111009);
}
.jawak-section-header { text-align: center; margin-bottom: 52px; padding: 0 20px; }
.jawak-pill {
  display: inline-flex; align-items: center; gap: 6px;
  padding: 8px 18px; margin-bottom: 14px; border-radius: 999px;
  background: linear-gradient(180deg, #1c1810, #221a12);
  border: 1px solid rgba(218,174,73,.28);
  color: var(--g); font-weight: 700; font-size: 13px;
}
.jawak-h2 {
  color: #fff3cf !important;
  font-size: clamp(1.6rem, 3.5vw, 2.5rem) !important;
  font-weight: 800 !important; margin: 0 0 12px !important;
}
.jawak-lead { color: var(--txs); font-size: 15px; max-width: 520px; margin: 0 auto; line-height: 1.9; }

.jawak-interactive-shell {
  display: grid; grid-template-columns: 400px 1fr; gap: 28px;
  align-items: stretch; max-width: 1180px; margin: 0 auto; padding: 24px;
  background: linear-gradient(180deg, #1c1812, #111009);
  border: 1px solid rgba(218,174,73,.16); border-radius: 26px;
  box-shadow: 0 16px 40px rgba(0,0,0,.2); position: relative; overflow: hidden;
}
.jawak-interactive-shell::before {
  content: ''; position: absolute; top: 0; left: 0; right: 0; height: 1px;
  background: linear-gradient(90deg, transparent, rgba(218,174,73,.32) 50%, transparent);
  pointer-events: none;
}
.jawak-media-card {
  border-radius: 20px; overflow: hidden;
  border: 1px solid rgba(218,174,73,.16); background-color: #0d0d0d; min-height: 420px;
}
.jawak-media-card video { width: 100%; height: 100%; min-height: 420px; object-fit: cover; display: block; }
.jawak-content-side { display: flex; flex-direction: column; justify-content: center; }
.jawak-top-badge {
  display: inline-flex; width: fit-content; padding: 8px 16px; margin-bottom: 12px;
  border-radius: 999px; background: linear-gradient(180deg, #1c1810, #221a12);
  border: 1px solid rgba(218,174,73,.28); color: var(--g); font-weight: 700; font-size: 13px;
}
.jawak-main-title { color: #fff3cf !important; font-size: clamp(1.4rem, 2.5vw, 2rem) !important; font-weight: 800 !important; margin: 0 0 10px !important; }
.jawak-subtitle { color: var(--txs); font-size: 14.5px; line-height: 1.9; margin: 0 0 18px; }
.jawak-tabs { display: grid; grid-template-columns: 1fr 1fr; gap: 10px; margin-bottom: 16px; }
.jawak-tab-btn {
  display: flex; align-items: center; gap: 10px;
  min-height: 58px; border-radius: 14px; padding: 12px 14px;
  background-color: rgba(255,255,255,.02); border: 1px solid rgba(218,174,73,.14);
  color: #ecd9a0; cursor: pointer; text-align: right; width: 100%;
  transition: all .25s var(--ease); font-size: 14px; font-weight: 700; font-family: inherit;
}
.jawak-tab-btn:hover { border-color: rgba(218,174,73,.26); transform: translateY(-1px); }
.jawak-tab-btn.active {
  background-color: rgba(218,174,73,.1); border-color: rgba(218,174,73,.36); color: #fff3cf;
  box-shadow: 0 6px 18px rgba(0,0,0,.12), 0 0 14px rgba(218,174,73,.07);
}
.jawak-tab-icon { display: flex; align-items: center; justify-content: center; }
.jawak-tab-icon svg { width: 22px; height: 22px; stroke: var(--g); fill: none; }
.jawak-feature-view {
  border-radius: 16px; background-color: rgba(255,255,255,.02);
  border: 1px solid rgba(218,174,73,.12); padding: 18px;
}
.jawak-feature-view.is-animating { animation: jawakFadeUp .3s var(--ease) both; }
@keyframes jawakFadeUp { from { opacity:0; transform:translateY(8px); } to { opacity:1; transform:none; } }
.jawak-feature-title { color: #fff3cf !important; font-size: clamp(1.1rem, 2vw, 1.45rem) !important; font-weight: 800 !important; margin: 0 0 8px !important; }
.jawak-feature-desc { color: var(--txs); font-size: 14px; line-height: 1.9; margin: 0 0 14px; }
.jawak-buy-btn {
  display: inline-flex; align-items: center; justify-content: center; gap: 8px;
  min-height: 46px; padding: 0 24px; border-radius: 999px;
  background: linear-gradient(135deg, var(--gx), var(--g), var(--gl));
  border: none; color: #0a0800 !important; font-weight: 800; font-size: 14px;
  text-decoration: none !important;
  box-shadow: 0 8px 22px rgba(218,174,73,.2);
  transition: transform .25s var(--ease), box-shadow .25s var(--ease);
}
.jawak-buy-btn:hover { transform: translateY(-2px); box-shadow: 0 14px 30px rgba(218,174,73,.32); color: #0a0800 !important; }

@media (max-width: 1100px) {
  .jawak-interactive-shell { grid-template-columns: 1fr; max-width: 660px; }
  .jawak-media-card, .jawak-media-card video { min-height: 260px; }
}
@media (max-width: 768px) {
  .jawak-interactive-block { padding: 48px 0; }
  .jawak-interactive-shell { padding: 14px; gap: 16px; }
}

/* ============================================================
   FAQ  — Alpine.js based (NOT Bootstrap accordion)
   Selectors from actual HTML: .faq-item > .faq-button > .faq-question / .faq-answer
   ============================================================ */
.faq-widget-section { background-color: transparent !important; }
.faq-widget { max-width: 700px !important; margin: 0 auto !important; padding: 0 16px !important; }

/* FAQ header icon area */
.lina-icon-wrap { color: var(--g) !important; }
.lina-glow {
  position: absolute; inset: 0; border-radius: 50%;
  box-shadow: 0 0 20px rgba(218,174,73,.3); background: rgba(218,174,73,.06);
}
.lina-star { color: rgba(218,174,73,.5) !important; fill: currentColor !important; }
.lina-icon svg { stroke: var(--g) !important; }

.faq-title { color: var(--txt) !important; }
.faq-subtitle { color: var(--txm) !important; }

/* FAQ list — all items equal width */
.faq-list {
  list-style: none !important; padding: 0 !important; margin: 0 !important;
  display: flex !important; flex-direction: column !important; gap: 10px !important;
}

.faq-item {
  background-color: var(--bg2) !important;
  border: 1px solid rgba(218,174,73,.15) !important;
  border-radius: 14px !important;
  overflow: hidden !important;
  width: 100% !important;
  box-sizing: border-box !important;
  transition: border-color .2s ease, box-shadow .2s ease !important;
}
.faq-item:hover {
  border-color: rgba(218,174,73,.28) !important;
  box-shadow: 0 4px 16px rgba(218,174,73,.06) !important;
}

/* faq-button = the clickable row */
.faq-button {
  display: flex !important;
  align-items: center !important;
  gap: 12px !important;
  padding: 16px 18px !important;
  cursor: pointer !important;
  background-color: var(--bg2) !important;
  border: none !important;
  width: 100% !important;
  box-sizing: border-box !important;
  text-align: right !important;
  direction: rtl !important;
  transition: background-color .2s ease !important;
}
.faq-button:hover { background-color: rgba(255,255,255,.02) !important; }
.faq-button > span svg { stroke: rgba(218,174,73,.6) !important; flex-shrink: 0 !important; }
.faq-button.open > span svg { stroke: var(--g) !important; }

.faq-question {
  color: #f0e8cc !important;
  font-weight: 700 !important;
  font-size: 14.5px !important;
  display: block !important;
  margin-bottom: 0 !important;
  background-color: transparent !important;
}
.faq-button.open .faq-question,
.faq-question.open { color: var(--g) !important; }

.faq-answer {
  color: var(--txs) !important;
  font-size: 14px !important;
  line-height: 1.9 !important;
  padding: 0 18px 16px 18px !important;
  background-color: var(--bg2) !important;
}

/* ============================================================
   FOOTER
   ============================================================ */
.store-section.footer-store-section, footer, .footer-area {
  background-color: var(--bg4) !important;
  position: relative !important;
}

 .store-section.footer-store-section, .col-lg-12 {
        color: #ffffff;
    }

.store-section.footer-store-section::before, footer::before {
  content: '';
  position: absolute; top: 0; left: 0; right: 0; height: 2px;
  background: linear-gradient(90deg, transparent, var(--g) 30%, var(--gl) 50%, var(--g) 70%, transparent);
  box-shadow: 0 0 18px rgba(218,174,73,.28);
}
/* Footer section titles — gold & prominent */
.store-section.footer-store-section h4,
.store-section.footer-store-section h5,
.store-section.footer-store-section h3,
.store-section.footer-store-section .footer-widget-title,
.store-section.footer-store-section .widget-title,
footer h4, footer h5, footer h3,
footer .footer-widget-title, footer .widget-title {
  color: var(--g) !important;
  font-weight: 800 !important;
  font-size: 15px !important;
  padding-bottom: 10px !important;
  border-bottom: 1px solid rgba(218,174,73,.14) !important;
  margin-bottom: 14px !important;
}
.store-section.footer-store-section a, footer a { color: #ffffff !important; }
.store-section.footer-store-section a:hover, footer a:hover { color: var(--g) !important; }
.store-section.footer-store-section p,
.store-section.footer-store-section span,
footer p, footer span,
footer li { color: var(--txm) !important; }

.store-section.footer-store-section p, .store-section.footer-store-section span, footer p, footer span, footer li {
    color: #ffffff !important;
}
    .store-section.footer-store-section.justify-content-lg-start {
        color: #eacc6b;
    }

/* Social icons */
.footer-social a, .social-links a {
  display: inline-flex !important; align-items: center !important; justify-content: center !important;
  width: 38px !important; height: 38px !important; border-radius: 8px !important;
  border: 1px solid rgba(218,174,73,.22) !important;
  background-color: rgba(218,174,73,.04) !important;
  color: var(--txs) !important; transition: all .25s ease !important;
}
.footer-social a:hover, .social-links a:hover {
  border-color: rgba(218,174,73,.5) !important;
  background-color: rgba(218,174,73,.1) !important;
  color: var(--g) !important; transform: translateY(-2px) !important;
}

/* Payment icons */
.payment-methods img, .footer-payments img {
  filter: grayscale(1) brightness(.65) !important;
  border: 1px solid rgba(218,174,73,.14) !important;
  border-radius: 6px !important; padding: 4px 8px !important;
}
.payment-methods img:hover { filter: grayscale(0) brightness(1) !important; }

/* ============================================================
   PRODUCT PAGE —  MODE
   ============================================================ */
/* Splide slider on product page */
.product-image-slide .splide,
.splide.bg-white {
  background-color: var(--bg2) !important;
  border-color: var(--bdr) !important;
}
.splide__slide { background-color: transparent !important; }
.splide__arrow {
  background-color: var(--bg3) !important;
  border: 1px solid var(--bdr) !important;
}
.splide__arrow svg { fill: var(--g) !important; }
.splide__pagination__page { background-color: var(--txm) !important; }
.splide__pagination__page.is-active { background-color: var(--g) !important; }

/* Product details wrapper */
.product-details-wrapper {
  background-color: var(--bg2) !important;
  border-color: var(--bdr) !important;
}
.product-details-wrapper .text-dark { color: var(--txt) !important; }

/* Prices */
.pr_price { color: var(--g) !important; font-weight: 800 !important; }
.pr_old-price { color: var(--txm) !important; text-decoration: line-through !important; }

/* Quantity input */
.productQty {
  background-color: var(--bg3) !important;
  border: 1px solid var(--bdr) !important;
  border-radius: 12px !important;
}
.productQty .substract, .productQty .plus { color: var(--g) !important; }
.productQty .quantityInput {
  background-color: transparent !important; border: none !important; color: var(--txt) !important;
}

/* Add to cart / Buy buttons */
.btn-main {
  background: linear-gradient(135deg, var(--gx), var(--g), var(--gl)) !important;
  border: none !important; color: #0a0800 !important; font-weight: 800 !important;
  border-radius: 999px !important;
  box-shadow: 0 8px 22px rgba(218,174,73,.22) !important;
}
.btn-main:hover { transform: translateY(-2px) !important; }
.btn-main-outline {
  background-color: transparent !important;
  border: 1px solid rgba(218,174,73,.5) !important;
  color: var(--g) !important; border-radius: 999px !important; font-weight: 700 !important;
}
.btn-main-outline:hover {
  background: linear-gradient(135deg, var(--gx), var(--g), var(--gl)) !important;
  border-color: transparent !important; color: #0a0800 !important;
}


.btn-direct-wa:hover { transform: translateY(-2px) !important; color: #0a0800 !important; }
.btn-direct-wa svg, .btn-direct-wa i { color: #0a0800 !important; fill: #0a0800 !important; }

/* Product share bar */
.product-share {
  background-color: var(--bg3) !important;
  border-color: var(--bdr) !important;
  border-radius: 14px !important;
}
.product-share a { color: var(--txs) !important; }
.product-share a:hover { color: var(--g) !important; }

/* Tab nav (description/reviews) */
.nav-tabs {
  border-color: rgba(218,174,73,.15) !important;
  background-color: var(--bg2) !important;
  border-radius: 999px !important; padding: 4px !important;
}
.nav-tabs .nav-link {
  color: var(--txs) !important; border: none !important;
  border-radius: 999px !important; background-color: transparent !important;
  padding: 10px 24px !important;
}
.nav-tabs .nav-link.active {
  background-color: var(--bg3) !important; color: var(--g) !important;
  border: 1px solid rgba(218,174,73,.25) !important;
}
.tab-content { background-color: var(--bg) !important; }
.tab-pane { background-color: var(--bg) !important; color: var(--txs) !important; }
.tab-pane * { color: #ffffff !important; }

.gl-star-rating--stars span {
    color: #ffffff!important;
}


/* ============================================================
   CART POPUP / MINI-CART  (white → dark)
   ============================================================ */
/* Dropdown menus (account, cart, etc.) */
.dropdown-menu {
  background-color: var(--bg2) !important;
  border: 1px solid rgba(218,174,73,.18) !important;
  border-radius: 14px !important;
  box-shadow: 0 12px 32px rgba(0,0,0,.5) !important;
}
.dropdown-item { color: var(--txs) !important; }
.dropdown-item:hover {
  background-color: rgba(218,174,73,.08) !important; color: var(--g) !important;
}
.dropdown-divider { border-color: rgba(218,174,73,.12) !important; }

/* Bootstrap Offcanvas (mobile nav / cart) */
.offcanvas, .offcanvas-end, .offcanvas-start, .offcanvas-top, .offcanvas-bottom {
  background-color: var(--bg2) !important;
  border-color: rgba(218,174,73,.14) !important;
}
.offcanvas-header {
  background-color: var(--bg3) !important;
  border-bottom: 1px solid rgba(218,174,73,.12) !important;
}
.offcanvas-title { color: var(--txt) !important; font-weight: 700 !important; }
.btn-close { filter: brightness(0) invert(1) !important; opacity: .6 !important; }
.btn-close:hover { opacity: 1 !important; }

/* Cart specific inside offcanvas */
.offcanvas .nav-link { color: var(--txs) !important; }
.offcanvas .navbar-nav .nav-link { color: var(--txt) !important; }

/* Custom Tjar cart popup / remove-cart-popup */
[class*="cart-popup"],
[id*="cart-popup"],
[class*="remove-cart"],
[id*="remove-cart"],
.cart-drop, .cart-dropdown,
.cart-popup-wrap, .cart-popup-modal,
.mini-cart, .mini_cart_item {
  background-color: var(--bg2) !important;
  border-color: rgba(218,174,73,.14) !important;
  border-radius: 16px !important;
}
/* Sweetalert / modal popups */
.swal2-popup, .swal2-modal {
  background-color: var(--bg2) !important;
  border: 1px solid rgba(218,174,73,.2) !important;
  border-radius: 20px !important;
}
.swal2-title { color: var(--txt) !important; }
.swal2-html-container { color: var(--txs) !important; }
.swal2-confirm {
  background: linear-gradient(135deg, var(--gx), var(--g), var(--gl)) !important;
  color: #0a0800 !important; font-weight: 800 !important; border-radius: 999px !important;
}

/* Generic popup/modal */
.modal-content {
  background-color: var(--bg2) !important;
  border: 1px solid rgba(218,174,73,.18) !important;
  border-radius: 18px !important;
}
.modal-header {
  background-color: var(--bg3) !important;
  border-bottom: 1px solid rgba(218,174,73,.12) !important;
}
.modal-title { color: var(--txt) !important; }
.modal-footer {
  background-color: var(--bg3) !important;
  border-top: 1px solid rgba(218,174,73,.12) !important;
}
.modal-body { background-color: var(--bg2) !important; color: var(--txs) !important; }

/* Popup user profile widget (white box image_1778639436195) */
.user-info, .profile-popup, .profile-drop, .account-popup,
[class*="account-drop"], [class*="user-drop"],
[class*="profile-drop"] {
  background-color: var(--bg2) !important;
  border: 1px solid rgba(218,174,73,.16) !important;
  border-radius: 16px !important;
  box-shadow: 0 12px 32px rgba(0,0,0,.5) !important;
}
.user-info *, .profile-popup * { color: var(--txs) !important; }
.user-info h5, .user-info h6, .profile-popup h5, .profile-popup h6 { color: var(--txt) !important; }
.user-info img, .profile-popup img {
  border: 2px solid rgba(218,174,73,.3) !important; border-radius: 50% !important;
}
.user-info .btn, .profile-popup .btn {
  border-color: rgba(218,174,73,.3) !important; color: var(--g) !important;
  border-radius: 999px !important;
}

/* ============================================================
   CART PAGE
   ============================================================ */
.page-cart, .cart-page, [class*="cart-section"] {
  background-color: var(--bg) !important;
}
/* Cart items — ensure images show normally, just dark card bg */
.page-cart .card, .cart-page .card,
[class*="cart-section"] .card {
  background-color: var(--bg2) !important;
  border: 1px solid rgba(218,174,73,.12) !important;
  border-radius: 16px !important;
}
/* Cart product image — ensure proper sizing */
.page-cart .card img, .cart-page .card img {
  border-radius: 12px !important;
  object-fit: contain !important;
  max-height: 90px !important;
  width: auto !important;
}
.page-cart .text-dark, .cart-page .text-dark { color: var(--txt) !important; }
.page-cart h1, .page-cart h2, .page-cart h3, .page-cart h4,
.cart-page h1, .cart-page h2, .cart-page h3, .cart-page h4 { color: var(--txt) !important; }
.cart-total-box, .order-summary {
  background-color: var(--bg2) !important;
  border: 1px solid rgba(218,174,73,.14) !important;
  border-radius: 16px !important;
}

/* Cart quantity toggle */
.cart-qty-toggle {
  background-color: var(--bg3) !important;
  border: 1px solid rgba(218,174,73,.2) !important;
  border-radius: 999px !important;
}

/* Cart proceed button */
.cart-checkout-btn, .btn-checkout,
[class*="checkout-btn"] {
  background: linear-gradient(135deg, var(--gx), var(--g), var(--gl)) !important;
  border: none !important; color: #0a0800 !important;
  font-weight: 800 !important; border-radius: 999px !important;
  box-shadow: 0 8px 22px rgba(218,174,73,.22) !important;
}

/* ============================================================
   CHECKOUT PAGE — Full Dark Gold Theme
   Real classes from /shop/checkout HTML source confirmed.
   ============================================================ */

/* ── Page background ── */
body.checkout_page,
.checkout_page body,
.checkout_page .checkout {
  background-color: var(--bg) !important;
}
.checkout {
  background-color: var(--bg) !important;
}

/* ── Main cards: checkout_head / checkout_cart / checkout_details ── */
.checkout_head,
.checkout_cart,
.checkout_details {
  background-color: var(--bg2) !important;
  border: 1px solid rgba(218,174,73,.14) !important;
  box-shadow: 0 4px 24px rgba(0,0,0,.35) !important;
}

/* ── Typography ── */
.checkout_page h1, .checkout_page h2,
.checkout_page h3, .checkout_page h4,
.checkout_page h5, .checkout_page h6,
.checkout_cart h4, .checkout_cart h5,
.checkout_details h4, .checkout_details h5,
.checkout_head h4, .checkout_head h5 {
  color: var(--txt) !important;
}
.checkout_page .text-dark,
.checkout_page .fw-500,
.checkout_page .fw-600 { color: var(--txt) !important; }
.checkout_page .text-muted,
.checkout_page .rsf-16.text-muted { color: var(--txm) !important; }
.checkout_page .text-danger { color: #e57373 !important; }

/* ── Back-to-cart button ── */
.back_cart,
.checkout_head .back_cart {
  background-color: rgba(218,174,73,.1) !important;
  color: var(--g) !important;
  border: 1px solid rgba(218,174,73,.25) !important;
  border-radius: 999px !important;
}
.back_cart:hover {
  background-color: rgba(218,174,73,.2) !important;
  color: #f0d878 !important;
}
.back_cart i { color: inherit !important; }

/* ── Order summary items strip ── */
.checkout_cart-items {
  border-color: rgba(218,174,73,.15) !important;
}
.checkout_cart-items .col,
.checkout_cart-items span { color: var(--txs) !important; }

/* ── Total row ── */
.checkout_cart-total { color: var(--g) !important; }
.checkout_cart-total h5 { color: var(--g) !important; }
.checkout_cart-total span { color: var(--g) !important; }

/* ── Coupon promo section ── */
.coupon_promo-card {
  background-color: var(--bg3) !important;
  border: 1px solid rgba(218,174,73,.18) !important;
  border-radius: 12px !important;
  padding: 16px !important;
}
.coupon_promo-title { color: var(--txs) !important; }
.coupon_promo-icon svg path { stroke: var(--g) !important; fill: var(--g) !important; }
.coupon_promo-input .form-control {
  background-color: var(--bg) !important;
  border-color: rgba(218,174,73,.2) !important;
  color: var(--txt) !important;
}
.coupon_promo-input .form-control::placeholder { color: rgba(218,174,73,.4) !important; }
/* Apply button */
.coupon_promo-input .btn-copy,
.coupon_promo-input .btn.btn-main {
  background: linear-gradient(135deg, var(--gx), var(--g), var(--gl)) !important;
  border: none !important;
  color: #0a0800 !important;
  font-weight: 700 !important;
  border-radius: 8px !important;
}

/* ── Step head icon circles ── */
.step_head-icon {
  background-color: rgba(218,174,73,.15) !important;
  border: 1.5px solid rgba(218,174,73,.35) !important;
  color: var(--g) !important;
}
/* Completed step (green → gold) */
.step_head-icon:not(.bg-dark) {
  background: linear-gradient(135deg, #b88b3c, #daae49) !important;
  border-color: transparent !important;
  color: #0a0700 !important;
}
/* Inactive step (bg-dark class) */
.step_head-icon.bg-dark {
  background-color: rgba(218,174,73,.12) !important;
  border-color: rgba(218,174,73,.2) !important;
  color: var(--txm) !important;
}

/* ── Step edit icon (large gold circle) ── */
.step_edit-icon {
  background: linear-gradient(135deg, var(--gx), var(--g)) !important;
  border-color: var(--gx) !important;
  color: #0a0700 !important;
  box-shadow: inset 0 0 0 3px var(--bg2), 0 0 0 1px var(--gx) !important;
}

/* ── Step body (inner card) ── */
.step_body {
  background-color: var(--bg) !important;
  border: 1px solid rgba(218,174,73,.1) !important;
  border-radius: 16px !important;
}
.step_edit-body {
  background-color: transparent !important;
}

/* ── hr divider ── */
.hr_line, .checkout_page hr {
  border-color: rgba(218,174,73,.14) !important;
  opacity: 1 !important;
}

/* ── btn-light-primary (edit / select buttons) ── */
.btn-light-primary {
  color: var(--g) !important;
}
.btn-light-primary::before {
  background: var(--g) !important;
  opacity: .12 !important;
}
.btn-light-primary:hover::before { opacity: .22 !important; }

/* ── btn-border-main ── */
.btn-border-main {
  border-color: var(--g) !important;
  color: var(--g) !important;
  background: none !important;
}

/* ── Saved address / shipping method cards ── */
.saved_address,
.new_address-bar,
.edit_address-wrap,
.address_form-wrap {
  background-color: var(--bg3) !important;
  border-color: rgba(218,174,73,.16) !important;
}
.saved_address-text, .saved_address label { color: var(--txt) !important; }
.new_address-bar label i { color: var(--g) !important; }

/* ── Shipping card ── */
.shipping_card {
  background-color: var(--bg3) !important;
  border-color: rgba(218,174,73,.16) !important;
}
.shipping_card:hover { border-color: rgba(218,174,73,.4) !important; }
.shipping_price {
  background-color: rgba(218,174,73,.1) !important;
  border-color: rgba(218,174,73,.2) !important;
  color: var(--g) !important;
}

/* ── Payment method cards ── */
.payment_tab,
.payment_tab-wrap .payment_tab {
  background-color: var(--bg3) !important;
  border-color: rgba(218,174,73,.16) !important;
  box-shadow: none !important;
}
.payment_tab:hover { border-color: rgba(218,174,73,.4) !important; }
.payment_name { color: var(--txt) !important; }

/* ── Forms: inputs, selects, textareas ── */
.checkout .form-control,
.checkout .form-select,
.checkout textarea,
.checkout_page .form-control,
.checkout_page .form-select {
  background-color: var(--bg3) !important;
  border-color: rgba(218,174,73,.18) !important;
  color: var(--txt) !important;
}
.checkout .form-control::placeholder,
.checkout_page .form-control::placeholder { color: rgba(218,174,73,.4) !important; }
.checkout .form-control:focus,
.checkout .form-select:focus,
.checkout_page .form-control:focus {
  border-color: rgba(218,174,73,.5) !important;
  box-shadow: 0 0 0 3px rgba(218,174,73,.1) !important;
  background-color: var(--bg3) !important;
}

/* ── Input group text ── */
.checkout .input-group-text,
.checkout_page .input-group-text {
  background-color: var(--bg) !important;
  border-color: rgba(218,174,73,.18) !important;
  color: var(--txm) !important;
}
.checkout .input-wrap i.icon,
.checkout_page .input-wrap i.icon { color: var(--txm) !important; }

/* ── Labels ── */
.checkout .form-label,
.checkout_page .form-label { color: var(--txs) !important; }

/* ── Custom radio buttons ── */
.custom_radio { box-shadow: 0 0 0 1.5px rgba(218,174,73,.3) !important; }
.custom_radio:hover { box-shadow: 0 0 0 1.5px var(--g), inset 0 0 1.5rem -1rem var(--g) !important; }
.custom_radio:checked {
  box-shadow: 0 0 0 1.5px var(--g) !important;
  background-color: var(--g) !important;
  border: 5px solid var(--bg2) !important;
}

/* ── Checkboxes ── */
.checkout_page .form-check-input,
.checkout .form-check-input {
  background-color: var(--bg3) !important;
  border-color: rgba(218,174,73,.3) !important;
}
.checkout_page .form-check-input:checked {
  background-color: var(--g) !important;
  border-color: var(--g) !important;
}

/* ── Spinner (loading) ── */
.checkout_page .spinner-border {
  color: var(--g) !important;
  border-color: rgba(218,174,73,.3) !important;
  border-top-color: var(--g) !important;
}

/* ── Login modal (guest checkout) ── */
.login_modal, .checkout_page .modal-content {
  background-color: var(--bg2) !important;
  border-color: rgba(218,174,73,.2) !important;
}

/* ── Select2 on checkout ── */
.checkout_page .select2-container--default .select2-selection--single {
  background-color: var(--bg3) !important;
  border-color: rgba(218,174,73,.18) !important;
  color: var(--txt) !important;
}
.checkout_page .select2-selection__rendered { color: var(--txt) !important; }

/* ── Checkout submit / place order button ── */
.checkout .btn-lg.btn-main,
.checkout_page .btn-lg.btn-main,
.checkout .submit-btn {
  background: linear-gradient(135deg, var(--gx), var(--g), var(--gl)) !important;
  border: none !important;
  color: #0a0700 !important;
  font-weight: 800 !important;
  border-radius: 12px !important;
  box-shadow: 0 6px 22px rgba(218,174,73,.25) !important;
}
.checkout .btn-lg.btn-main:hover,
.checkout_page .btn-lg.btn-main:hover { transform: translateY(-2px) !important; }

/* ── Guest login modal (phone/email/OTP screens) ── */
.checkout_page .modal-content,
.checkout_page .login_modal .modal-content {
  background-color: var(--bg2) !important;
  border: 1px solid rgba(218,174,73,.18) !important;
  border-radius: 20px !important;
}
/* Back-home arrow inside modal */
.login_back-home {
  color: rgba(218,174,73,.5) !important;
}
.login_back-home:hover { color: var(--g) !important; }

/* Phone/Email input screen */
.inputForm--screen,
.inputNameForm {
  background-color: transparent !important;
}
.account-form {
  background-color: transparent !important;
}
.form-title { color: var(--txt) !important; }
.form-sub   { color: var(--txm) !important; }

/* Toggle: "تسجيل الدخول بالبريد / الجوال" text buttons */
.btn-text {
  color: var(--g) !important;
  background: none !important;
  border: none !important;
  cursor: pointer !important;
  text-decoration: underline !important;
  text-underline-offset: 3px !important;
}
.btn-text:hover { color: #f0d878 !important; }

/* Submit button in guest form */
.submit-btn {
  background: linear-gradient(135deg, var(--gx), var(--g), var(--gl)) !important;
  border: none !important;
  color: #0a0700 !important;
  font-weight: 800 !important;
  border-radius: 10px !important;
  padding: 13px 20px !important;
  width: 100% !important;
  display: flex !important;
  align-items: center !important;
  justify-content: center !important;
  box-shadow: 0 6px 20px rgba(218,174,73,.22) !important;
}

/* ── OTP verification screen ── */
.OTPForm--screen { background-color: transparent !important; }

/* OTP boxes */
.otp-input {
  background-color: var(--bg3) !important;
  border-color: rgba(218,174,73,.25) !important;
  color: var(--txt) !important;
  font-size: 22px !important;
  font-weight: 700 !important;
  text-align: center !important;
  border-radius: 10px !important;
  width: 52px !important;
  height: 56px !important;
}
.otp-input:focus {
  border-color: var(--g) !important;
  box-shadow: 0 0 0 3px rgba(218,174,73,.15) !important;
  background-color: var(--bg3) !important;
}

/* "Change number" button */
.change_numb {
  color: var(--g) !important;
  background: none !important;
  border: none !important;
  cursor: pointer !important;
}
.change_numb i { color: inherit !important; }
.change_numb:hover { color: #f0d878 !important; }

/* OTP timer text */
.OTPForm--screen .text-muted { color: var(--txm) !important; }

/* ── Payment method tabs (lazy-loaded) ── */
.payment_tab {
  background-color: var(--bg3) !important;
  border: 1px solid rgba(218,174,73,.16) !important;
  border-radius: 12px !important;
  box-shadow: none !important;
  cursor: pointer !important;
  transition: border-color .2s ease !important;
}
.payment_tab:hover { border-color: rgba(218,174,73,.4) !important; }
.payment_tab.active,
.payment_tab[aria-selected="true"] {
  border-color: var(--g) !important;
  background-color: rgba(218,174,73,.06) !important;
}
.payment_name { color: var(--txt) !important; font-weight: 500 !important; }
.payment_tab .img img { filter: brightness(.85) !important; }

/* ── Shipping method cards (lazy-loaded) ── */
.shipping_card {
  background-color: var(--bg3) !important;
  border: 1px solid rgba(218,174,73,.16) !important;
  border-radius: 12px !important;
  cursor: pointer !important;
  transition: border-color .2s ease !important;
}
.shipping_card:hover { border-color: rgba(218,174,73,.4) !important; }
.shipping_info { color: var(--txt) !important; }
.shipping_price {
  background-color: rgba(218,174,73,.1) !important;
  border: 1px solid rgba(218,174,73,.2) !important;
  color: var(--g) !important;
  border-radius: 8px !important;
}

/* ── Saved address / new address cards (lazy-loaded) ── */
.saved_address,
.new_address-bar {
  background-color: var(--bg3) !important;
  border-color: rgba(218,174,73,.16) !important;
  border-radius: 12px !important;
}
.saved_address:hover,
.new_address-bar:hover { border-color: rgba(218,174,73,.35) !important; }
.saved_address-text { color: var(--txt) !important; }
.new_address-bar label i { color: var(--g) !important; }

/* Address / form wraps */
.edit_address-wrap,
.address_form-wrap {
  background-color: var(--bg3) !important;
  border-color: rgba(218,174,73,.16) !important;
  border-radius: 20px !important;
}

/* ── Error messages ── */
.checkout_page .text-danger,
[class*="error__"] { color: #e07070 !important; }

/* ── ch-border variable override for dark mode ── */
.checkout_page { --ch-border: rgba(218,174,73,.2) !important; }

/* ============================================================
   PAGE HEADERS — CATEGORY & INNER PAGES
   ============================================================ */
.page-header, .breadcrumb-area, .inner-banner,
.page-title-area, .category-page-banner {
  background-color: var(--bg) !important;
  border-bottom: 1px solid rgba(218,174,73,.1) !important;
  padding-top: calc(var(--bar-h) + var(--nav-h) + 16px) !important;
  margin-top: 0 !important;
}
.page-header h1, .page-header h2,
.breadcrumb-area h1, .breadcrumb-area h2 { color: var(--txt) !important; }

/* ============================================================
   SECTIONS — general dark backgrounds
   ============================================================ */
.widgetArea, section.widgetArea {
  background-color: transparent !important;
}
.product-cat-slider-area, .product-type-area {
  background-color: var(--bg) !important;
}
.section-head .section-title { color: var(--txt) !important; }
.section-head .section-sub { color: var(--g) !important; }

/* Remove white section backgrounds */
.py-md-5, .py-5 { background-color: transparent !important; }
section { background-color: transparent !important; }
.container { background-color: transparent !important; }

/* ============================================================
   GLOBAL BUTTONS
   ============================================================ */
.btn-primary, [class*="btn-primary"] {
  background: linear-gradient(135deg, var(--gx), var(--g), var(--gl)) !important;
  border: none !important; color: #0a0800 !important;
  font-weight: 800 !important; border-radius: 999px !important;
}
.btn-primary:hover { transform: translateY(-2px) !important; }
.btn-outline-secondary {
  border-color: rgba(218,174,73,.35) !important; color: var(--g) !important;
}

/* Toast notifications */
.toast, .toastr {
  background-color: var(--bg3) !important;
  border: 1px solid var(--bdr) !important;
  color: var(--txt) !important; border-radius: 12px !important;
}
.toast-success { border-color: rgba(218,174,73,.3) !important; }

/* Select2 */
.select2-container--default .select2-selection--single {
  background-color: var(--bg3) !important;
  border-color: rgba(218,174,73,.2) !important;
}
.select2-dropdown {
  background-color: var(--bg2) !important;
  border-color: rgba(218,174,73,.2) !important;
}
.select2-container--default .select2-results__option { color: var(--txs) !important; }
.select2-container--default .select2-results__option--highlighted {
  background-color: rgba(218,174,73,.1) !important; color: var(--g) !important;
}

/* ============================================================
   RESPONSIVE — MOBILE FIRST
   Tjar bottom nav bar height ≈ 60px — everything must respect it.
   ============================================================ */

/* ── Tablet & Mobile — Navbar dark background ──────── */
/* On tablet/mobile Tjar forces gold background — override to dark */
@media (max-width: 991px) {

  /* Navbar: always dark on mobile/tablet (never gold) */
  nav.navbar.navbar3, .custom-navbar, .navbar.navbar3 {
    background-color: rgba(12, 9, 0, 0.97) !important;
    backdrop-filter: none !important;
    -webkit-backdrop-filter: none !important;
    border-bottom: 1px solid rgba(218,174,73,.14) !important;
    box-shadow: 0 2px 16px rgba(0,0,0,.5) !important;
  }
  /* Scrolled = same dark on mobile */
  nav.navbar.navbar3.scrolled, .custom-navbar.scrolled {
    background-color: rgba(8, 6, 0, 0.99) !important;
  }

  /* Hamburger toggle button */
  .navbar-toggler {
    border: 1px solid rgba(218,174,73,.35) !important;
    border-radius: 8px !important;
    padding: 6px 10px !important;
    background-color: transparent !important;
  }
  .navbar-toggler:focus { box-shadow: 0 0 0 2px rgba(218,174,73,.2) !important; }
  /* Hamburger bars icon */
  .navbar-toggler-icon {
    background-image: url("data:image/svg+xml,%3csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 30 30'%3e%3cpath stroke='rgba%28218%2C174%2C73%2C1%29' stroke-linecap='round' stroke-miterlimit='10' stroke-width='2' d='M4 7h22M4 15h22M4 23h22'/%3e%3c/svg%3e") !important;
    width: 20px !important; height: 20px !important;
  }
  /* Fallback: any <i> or <span> inside toggler */
  .navbar-toggler i, .navbar-toggler span.icon,
  .nav_icon, [class*="nav-icon"], [class*="nav_icon"] {
    color: var(--g) !important;
    stroke: var(--g) !important;
  }
  /* nav_icon used in Tjar mobile menu button */
  .nav_icon svg, .nav_icon i { color: var(--g) !important; stroke: var(--g) !important; fill: var(--g) !important; }

  /* Search icon / bar on mobile */
  .navbar-input input, .searchbar-suggetions input {
    background-color: rgba(218,174,73,.08) !important;
    border: 1px solid rgba(218,174,73,.2) !important;
    color: #fff !important;
  }
  .navbar-input input::placeholder, .searchbar-suggetions input::placeholder {
    color: rgba(218,174,73,.5) !important;
  }
  .navbar-input svg, .searchbar-suggetions svg,
  .navbar-input i, .searchbar-suggetions i {
    color: var(--g) !important; stroke: var(--g) !important;
  }

  /* All nav icons (cart, wishlist, account, search) */
  nav.navbar.navbar3 .icon-track-navbar svg,
  nav.navbar.navbar3 .icon-track-navbar i,
  .custom-navbar .icon-track-navbar svg,
  .custom-navbar .icon-track-navbar i {
    color: var(--g) !important;
    stroke: var(--g) !important;
  }
  /* Cart count badge */
  .icon-notification {
    background-color: var(--g) !important;
    color: #000 !important;
    font-weight: 700 !important;
  }

  /* Logo */
  nav.navbar.navbar3 .navbar-brand img,
  .custom-navbar .navbar-brand img {
    filter: none !important;
    opacity: 1 !important;
  }

  /* Interactive block */
  .jawak-interactive-shell {
    grid-template-columns: 1fr !important;
    max-width: 660px !important;
  }
  .jawak-media-card,
  .jawak-media-card video { min-height: 260px !important; }

  /* Features */
  .four-blocks-grid {
    grid-template-columns: repeat(2, 1fr) !important;
    gap: 16px !important;
  }
}

/* ── Mobile (≤ 767px) ────────────────────── */
@media (max-width: 767px) {

  /* Variables — smaller promo bar on mobile */
  :root {
    --bar-h: 36px;
    --nav-h: 56px;
  }

  /* ── Promo bar ── */
  #jwk-bar { height: var(--bar-h) !important; }
  .jwk-item { font-size: 12px !important; padding: 0 14px !important; }
  .jwk-sep  { padding: 0 3px !important; font-size: 8px !important; }
  #jwk-close { width: 32px !important; font-size: 13px !important; }

  /* ── Navbar ── */
  nav.navbar.navbar3, .custom-navbar, .navbar.navbar3 {
    top: var(--bar-h) !important;
  }
  body.jwk-bar-closed nav.navbar.navbar3,
  body.jwk-bar-closed .custom-navbar { top: 0 !important; }

  /* ── Hero ── */
  .hero.hero-area {
    min-height: 100svh !important;
    min-height: 100vh !important;
  }
  .hero.hero-area .intro-excerpt h1,
  .hero.hero-area .intro-excerpt h2 {
    font-size: clamp(1.5rem, 7vw, 2.2rem) !important;
  }

  /* ── Brand logos ── */
  .brand-img {
    padding: 10px 12px !important;
    border-radius: 10px !important;
  }
  .brand-img img {
    max-height: 150x !important;
  }

  /* ── Splide (brand + product sliders) ── */
  .splide__arrow { width: 32px !important; height: 32px !important; }

  /* ── Product cards ── */
  .product-item, .card2 {
    border-radius: 12px !important;
  }

  /* ── Features grid ── */
  .four-blocks-grid {
    grid-template-columns: repeat(2, 1fr) !important;
    gap: 12px !important;
  }
  .four-blocks-box .single-promo {
    padding: 16px 12px !important;
    border-radius: 14px !important;
  }
  .single-promo-icon {
    width: 46px !important; height: 46px !important;
    border-radius: 12px !important;
    margin-bottom: 12px !important;
  }
  .single-promo-icon i { font-size: 20px !important; }
  .single-promo-icon svg { width: 20px !important; height: 20px !important; }
  .single-promo-contents-title { font-size: 14px !important; }
  .single-promo-contents-para  { font-size: 12.5px !important; }

  /* ── Interactive block ── */
  .jawak-interactive-block { padding: 40px 0 !important; }
  .jawak-section-header { margin-bottom: 30px !important; padding: 0 12px !important; }
  .jawak-interactive-shell {
    grid-template-columns: 1fr !important;
    padding: 14px !important; gap: 14px !important;
    border-radius: 18px !important;
    margin: 0 12px !important;
  }
  .jawak-media-card, .jawak-media-card video {
    min-height: 200px !important;
    border-radius: 14px !important;
  }
  .jawak-tabs { grid-template-columns: 1fr 1fr !important; gap: 8px !important; }
  .jawak-tab-btn { min-height: 50px !important; font-size: 13px !important; padding: 10px 10px !important; }
  .jawak-feature-view { padding: 14px !important; }

  /* ── FAQ ── */
  .faq-widget { padding: 0 12px !important; }
  .faq-button { padding: 14px 14px !important; }
  .faq-question { font-size: 13.5px !important; }
  .faq-answer { font-size: 13px !important; padding: 0 14px 14px !important; }

  /* ── Footer ── */
  .store-section.footer-store-section {
    /* Tjar's bottom nav is ~60px tall on mobile — add padding */
    padding-bottom: 70px !important;
  }

  /* ── Cart / Checkout pages ── */
  .page-cart .card, .cart-page .card {
    margin-bottom: 12px !important;
  }
  .page-cart .card img, .cart-page .card img {
    max-height: 70px !important;
  }


  /* ── WhatsApp floating button on phone   ──
@media (max-width: 992px) {
    ._wa {
        bottom: 90px!important;
        right: 12px!important;
    }
}

   
  /* ── Tjar mobile bottom nav ─
     Ensure it stays on top and dark             */
  .navbar-bottom, .bottom-navbar, .mobile-navbar,
  [class*="bottom-nav"], [class*="bottom_nav"],
  .layout-footer-nav, .app-bottom-nav {
    background-color: var(--bg2) !important;
    border-top: 1px solid rgba(218,174,73,.14) !important;
    z-index: 10000 !important;
  }
  .navbar-bottom a, .bottom-navbar a,
  [class*="bottom-nav"] a { color: var(--txm) !important; }
  .navbar-bottom a.active, .bottom-navbar a.active,
  [class*="bottom-nav"] a.active { color: var(--g) !important; }
  .navbar-bottom svg, .bottom-navbar svg,
  [class*="bottom-nav"] svg { stroke: currentColor !important; }

  /* Page header on inner pages (mobile) */
  .page-header, .breadcrumb-area, .inner-banner {
    padding-top: calc(var(--bar-h) + var(--nav-h) + 12px) !important;
  }

  /* Offcanvas menu (mobile nav drawer) */
  .offcanvas { width: 85vw !important; max-width: 320px !important; }

  /* General container padding */
  .container { padding-left: 14px !important; padding-right: 14px !important; }

  /* Section spacing */
  section { margin-bottom: 0 !important; }
}

/* ── Small mobile (≤ 480px) ─────────────── */
@media (max-width: 480px) {

  /* Features: single column on very small screens */
  .four-blocks-grid { grid-template-columns: 1fr !important; gap: 10px !important; }
  .four-blocks-box .single-promo {
    flex-direction: row !important; align-items: flex-start !important;
    text-align: right !important; gap: 14px !important;
    padding: 16px 14px !important;
  }
  .single-promo-icon { margin-bottom: 0 !important; flex-shrink: 0 !important; }

  /* Hero text */
  .hero.hero-area .intro-excerpt h1,
  .hero.hero-area .intro-excerpt h2 { font-size: clamp(1.3rem, 6vw, 1.8rem) !important; }

  /* Brand logos */
  .brand-img { padding: 8px 10px !important; }
  .brand-img img { max-height: 50px !important; }

  /* Interactive tabs: 2-col still ok at 480 */
  .jawak-tabs { grid-template-columns: 1fr 1fr !important; }
  .jawak-tab-btn { font-size: 12px !important; padding: 8px 8px !important; min-height: 44px !important; }

  /* Promo bar: tighter */
  .jwk-item { font-size: 11.5px !important; padding: 0 10px !important; }
}

/* ── Landscape mobile (height ≤ 500px) ───── */
@media (max-height: 500px) and (max-width: 900px) {
  .hero.hero-area { min-height: 100vw !important; }
  #jwk-bar { height: 30px !important; }
  :root { --bar-h: 30px; }
  .jwk-item { font-size: 11px !important; }
}

/* ============================================================
   TJAR MOBILE BOTTOM NAVIGATION — Dark Gold Theme
   Classes confirmed from real HTML: .mobile-nav, .mobile-nav-item,
   .mobile-nav-link, .mobile-nav-icon, .mobile-nav-title, .icon-count
   ============================================================ */

/* Container */
div.mobile-nav:not(.opacity-0) {
  background-color: #231c06 !important;
  border-top: 1px solid rgba(218,174,73,.28) !important;
  box-shadow: 0 -4px 24px rgba(0,0,0,.6) !important;
}

/* Each nav item */
div.mobile-nav:not(.opacity-0) .mobile-nav-item {
  position: relative !important;
  flex: 1 1 0 !important;
}

/* Link wrapper */
div.mobile-nav:not(.opacity-0) .mobile-nav-link {
  display: flex !important;
  flex-direction: column !important;
  align-items: center !important;
  justify-content: center !important;
  gap: 3px !important;
  padding: 8px 4px !important;
  color: rgba(218,174,73,.55) !important;
  text-decoration: none !important;
  transition: color .2s ease !important;
  position: relative !important;
}

/* Hover & active */
div.mobile-nav:not(.opacity-0) .mobile-nav-link:hover,
div.mobile-nav:not(.opacity-0) .mobile-nav-link.active,
div.mobile-nav:not(.opacity-0) .mobile-nav-link[class*="active"],
div.mobile-nav:not(.opacity-0) .mobile-nav-item.active .mobile-nav-link {
  color: #daae49 !important;
}

/* Gold top-indicator bar on active */
div.mobile-nav:not(.opacity-0) .mobile-nav-link.active::before,
div.mobile-nav:not(.opacity-0) .mobile-nav-item.active .mobile-nav-link::before {
  content: '' !important;
  position: absolute !important;
  top: 0 !important; left: 20% !important; right: 20% !important;
  height: 2px !important;
  background: linear-gradient(90deg, #b88b3c, #daae49, #f0d878) !important;
  border-radius: 0 0 4px 4px !important;
}

/* Icons */
div.mobile-nav:not(.opacity-0) .mobile-nav-icon {
  font-size: 22px !important;
  line-height: 1 !important;
  position: relative !important;
  display: flex !important;
  align-items: center !important;
  justify-content: center !important;
}
div.mobile-nav:not(.opacity-0) .mobile-nav-icon i {
  font-size: 22px !important;
  color: inherit !important;
}

/* Labels */
div.mobile-nav:not(.opacity-0) .mobile-nav-title {
  font-size: 10px !important;
  font-weight: 500 !important;
  color: inherit !important;
  white-space: nowrap !important;
  font-family: 'Cairo', 'Tajawal', sans-serif !important;
}

/* Badge (cart / wishlist count) */
div.mobile-nav:not(.opacity-0) .icon-count,
div.mobile-nav:not(.opacity-0) [class*="icon-count"],
div.mobile-nav:not(.opacity-0) .badge,
div.mobile-nav:not(.opacity-0) [x-text*="cart"],
div.mobile-nav:not(.opacity-0) [wishlistCount] {
  position: absolute !important;
  top: -6px !important; right: -8px !important;
  min-width: 17px !important; height: 17px !important;
  background: linear-gradient(135deg, #b88b3c, #daae49) !important;
  color: #0a0700 !important;
  font-size: 9px !important;
  font-weight: 800 !important;
  border-radius: 999px !important;
  display: flex !important;
  align-items: center !important;
  justify-content: center !important;
  padding: 0 3px !important;
  border: 1.5px solid #0d0a00 !important;
  line-height: 1 !important;
}

/* Home icon center highlight pill */
div.mobile-nav:not(.opacity-0) .mobile-nav-item:nth-child(3) .mobile-nav-link,
div.mobile-nav:not(.opacity-0) .mobile-nav-item.center-nav .mobile-nav-link {
  color: #daae49 !important;
}
div.mobile-nav:not(.opacity-0) .mobile-nav-item:nth-child(3) .mobile-nav-icon i,
div.mobile-nav:not(.opacity-0) .mobile-nav-item.center-nav .mobile-nav-icon i {
  font-size: 26px !important;
  filter: drop-shadow(0 0 6px rgba(218,174,73,.45)) !important;
}

/* ============================================================
   USER DASHBOARD — /user-home — Dark Gold Theme
   Classes confirmed from real HTML source.
   External CSS: user-dashboard.css (overridden here)
   ============================================================ */

/* ── Page & section wrapper ── */
.dash_section {
  background-color: var(--bg) !important;
  min-height: 100vh !important;
}
.dash_container { background-color: transparent !important; }

/* ── Main head card (cover + welcome + menu) ── */
.dash_head {
  background-color: var(--bg2) !important;
  border: 1px solid rgba(218,174,73,.14) !important;
  border-radius: 24px !important;
  box-shadow: 0 6px 32px rgba(0,0,0,.4) !important;
  overflow: hidden !important;
}

/* ── Cover / banner area ── */
.dash_user-cover {
  background-color: var(--bg3) !important;
  border-bottom: 1px solid rgba(218,174,73,.12) !important;
  position: relative !important;
}
/* Decorative inner cover — subtle gold tint */
.dash_inner-cover {
  background-image: none !important;
  background: linear-gradient(135deg,
    rgba(184,139,60,.08) 0%,
    rgba(218,174,73,.05) 50%,
    rgba(10,8,0,.2) 100%) !important;
  border-bottom: 1px solid rgba(218,174,73,.1) !important;
}
/* Decorative background */
.dash_user-bg {
  background: radial-gradient(ellipse at 60% 50%,
    rgba(218,174,73,.06) 0%,
    transparent 70%) !important;
}
/* Shadow circle decoration */
.shadow_circle i {
  background: rgba(218,174,73,.05) !important;
}

/* ── Profile image ring ── */
.dash_user-img {
  border: 3px solid rgba(218,174,73,.4) !important;
  border-radius: 50% !important;
  box-shadow: 0 0 0 4px var(--bg2), 0 0 20px rgba(218,174,73,.15) !important;
}
.dash_user-img img {
  border-radius: 50% !important;
}

/* ── Logout button ── */
.dash_logout, .btn_2d.danger.dash_logout {
  background-color: rgba(220,53,69,.1) !important;
  border: 1px solid rgba(220,53,69,.25) !important;
  color: #e07070 !important;
  border-radius: 999px !important;
}
.dash_logout svg { fill: #e07070 !important; }
.dash_logout:hover {
  background-color: rgba(220,53,69,.2) !important;
  border-color: rgba(220,53,69,.4) !important;
}

/* ── Welcome text ── */
.dash_welcome {
  background-color: transparent !important;
  text-align: center !important;
}
.dash_welcome h2 {
  color: var(--txt) !important;
  font-size: clamp(1.2rem, 3vw, 1.6rem) !important;
}
.dash_welcome h2 .emoji { font-style: normal !important; }
.dash_welcome span {
  color: var(--txm) !important;
  font-size: 14px !important;
}

/* ── Tabs menu wrapper ── */
.dash_menu-wrap {
  background-color: transparent !important;
  border-top: 1px solid rgba(218,174,73,.1) !important;
  position: relative !important;
}

/* ── Tab list (horizontal scroll) ── */
.dash_menu-list {
  background-color: transparent !important;
  list-style: none !important;
  display: flex !important;
  gap: 4px !important;
  padding: 8px 12px !important;
  overflow-x: auto !important;
  scrollbar-width: none !important;
}
.dash_menu-list::-webkit-scrollbar { display: none !important; }

/* ── Each tab item ── */
.dash_menu-list li {
  flex-shrink: 0 !important;
}
.dash_menu-list li a {
  display: flex !important;
  align-items: center !important;
  gap: 6px !important;
  padding: 8px 14px !important;
  border-radius: 999px !important;
  color: var(--txm) !important;
  font-size: 13px !important;
  font-weight: 500 !important;
  white-space: nowrap !important;
  text-decoration: none !important;
  transition: all .2s ease !important;
  border: 1px solid transparent !important;
}
.dash_menu-list li a:hover {
  color: var(--g) !important;
  background-color: rgba(218,174,73,.08) !important;
  border-color: rgba(218,174,73,.18) !important;
}

/* ── Active tab ── */
.dash_menu-list li.active a,
.dash_menu-list li a.active {
  color: #0a0700 !important;
  background: linear-gradient(135deg, var(--gx), var(--g), var(--gl)) !important;
  border-color: transparent !important;
  font-weight: 700 !important;
  box-shadow: 0 3px 12px rgba(218,174,73,.25) !important;
}

/* ── Tab SVG icons ── */
.dash_menu-list li a svg,
.dash_menu-list li a .default_icon,
.dash_menu-list li a .activ_icon {
  width: 16px !important;
  height: 16px !important;
  flex-shrink: 0 !important;
}
/* Normal tab: icon muted gold */
.dash_menu-list li:not(.active) a svg,
.dash_menu-list li:not(.active) a .default_icon {
  fill: var(--txm) !important;
  color: var(--txm) !important;
}
/* Active tab: icon dark (on gold bg) */
.dash_menu-list li.active a svg,
.dash_menu-list li.active a .activ_icon {
  fill: #0a0700 !important;
  color: #0a0700 !important;
}
/* Show/hide logic: default shows default_icon, active shows activ_icon */
.dash_menu-list li:not(.active) .activ_icon { display: none !important; }
.dash_menu-list li.active .default_icon { display: none !important; }
.dash_menu-list li.active .activ_icon { display: inline-block !important; }

/* ── Scroll arrows ── */
.dash_menu-arrow {
  background-color: rgba(218,174,73,.1) !important;
  border: 1px solid rgba(218,174,73,.2) !important;
  border-radius: 50% !important;
  width: 32px !important; height: 32px !important;
  display: flex !important;
  align-items: center !important;
  justify-content: center !important;
  cursor: pointer !important;
  flex-shrink: 0 !important;
  transition: background .2s !important;
}
.dash_menu-arrow:hover {
  background-color: rgba(218,174,73,.2) !important;
}
/* Override hardcoded fill="#1E1E1E" on SVG */
.dash_menu-arrow svg { fill: var(--g) !important; }
.dash_menu-arrow svg path { fill: var(--g) !important; }
.dash_menu-arrow.disabled { opacity: .3 !important; pointer-events: none !important; }

/* ── hr divider in head ── */
.dash_head .hr_line,
.dash_head hr {
  border-color: rgba(218,174,73,.12) !important;
  opacity: 1 !important;
}

/* ── Dashboard body ── */
.dash_body {
  background-color: transparent !important;
}

/* ── Overview cards container ── */
.dash_overview-wrap {
  display: grid !important;
  grid-template-columns: repeat(auto-fit, minmax(160px, 1fr)) !important;
  gap: 16px !important;
}

/* ── Each stat card ── */
.dash_overview-card {
  background-color: var(--bg2) !important;
  border: 1px solid rgba(218,174,73,.14) !important;
  border-radius: 20px !important;
  padding: 24px 20px !important;
  display: flex !important;
  flex-direction: column !important;
  align-items: center !important;
  text-align: center !important;
  gap: 10px !important;
  text-decoration: none !important;
  transition: transform .2s ease, border-color .2s ease, box-shadow .2s ease !important;
  box-shadow: 0 2px 12px rgba(0,0,0,.25) !important;
}
.dash_overview-card:hover {
  transform: translateY(-3px) !important;
  border-color: rgba(218,174,73,.35) !important;
  box-shadow: 0 8px 28px rgba(218,174,73,.1) !important;
  text-decoration: none !important;
}

/* ── Icon area in stat card ── */
.dash_overview-icon {
  width: 56px !important; height: 56px !important;
  border-radius: 16px !important;
  background: linear-gradient(135deg, rgba(184,139,60,.15), rgba(218,174,73,.1)) !important;
  border: 1px solid rgba(218,174,73,.2) !important;
  display: flex !important;
  align-items: center !important;
  justify-content: center !important;
  position: relative !important;
}
/* The decorative <span> inside icon */
.dash_overview-icon > span {
  position: absolute !important;
  inset: 0 !important;
  border-radius: inherit !important;
  background: radial-gradient(circle at 30% 30%, rgba(218,174,73,.12), transparent) !important;
}
/* SVG icon */
.dash_overview-icon svg {
  width: 26px !important; height: 26px !important;
  fill: var(--g) !important;
  color: var(--g) !important;
  position: relative !important;
  z-index: 1 !important;
  filter: drop-shadow(0 0 6px rgba(218,174,73,.3)) !important;
}

/* ── Stat number ── */
.dash_overview-heading {
  color: var(--g) !important;
  font-size: 1.5rem !important;
  font-weight: 700 !important;
  margin: 0 !important;
  line-height: 1.2 !important;
}
.dash_overview-heading sar,
.dash_overview-heading sar::before {
  font-size: .85em !important;
  color: var(--txm) !important;
}

/* ── Stat label ── */
.dash_overview-btn {
  color: var(--txs) !important;
  font-size: 13px !important;
  font-weight: 500 !important;
}

/* ── Other dashboard sub-pages (orders, downloads, etc.) ── */
.dash_body .card,
.dash_body .bg-white,
.dash_body [class*="order-card"],
.dash_body [class*="ticket-card"] {
  background-color: var(--bg2) !important;
  border-color: rgba(218,174,73,.14) !important;
  color: var(--txt) !important;
}
.dash_body table {
  background-color: transparent !important;
}
.dash_body thead th {
  background-color: var(--bg3) !important;
  color: var(--g) !important;
  border-color: rgba(218,174,73,.14) !important;
}
.dash_body tbody tr {
  border-color: rgba(218,174,73,.08) !important;
}
.dash_body tbody tr:hover {
  background-color: rgba(218,174,73,.04) !important;
}
.dash_body td, .dash_body th {
  color: var(--txs) !important;
  border-color: rgba(218,174,73,.1) !important;
}
.dash_body .badge { font-weight: 600 !important; }

/* Status badges in orders */
.dash_body .badge-success, .dash_body .bg-success { background-color: rgba(34,197,94,.15) !important; color: #4ade80 !important; }
.dash_body .badge-warning, .dash_body .bg-warning { background-color: rgba(218,174,73,.15) !important; color: var(--g) !important; }
.dash_body .badge-danger,  .dash_body .bg-danger  { background-color: rgba(220,53,69,.15) !important; color: #e07070 !important; }
.dash_body .badge-info,    .dash_body .bg-info    { background-color: rgba(99,179,237,.15) !important; color: #7ec8e3 !important; }

/* Pagination */
.dash_body .page-item .page-link {
  background-color: var(--bg3) !important;
  border-color: rgba(218,174,73,.15) !important;
  color: var(--txs) !important;
}
.dash_body .page-item.active .page-link {
  background: linear-gradient(135deg, var(--gx), var(--g)) !important;
  border-color: transparent !important;
  color: #0a0700 !important;
}

/* ── Mobile: stack cards 2-col ── */
@media (max-width: 767px) {
  .dash_overview-wrap {
    grid-template-columns: repeat(2, 1fr) !important;
    gap: 12px !important;
  }
  .dash_overview-card { padding: 18px 12px !important; }
  .dash_overview-heading { font-size: 1.2rem !important; }
  .dash_overview-icon { width: 46px !important; height: 46px !important; }
  .dash_overview-icon svg { width: 22px !important; height: 22px !important; }
}
